CREATE TABLE IF NOT EXISTS mock_attempts (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), user_id TEXT NOT NULL, test_id UUID NOT NULL, score NUMERIC NOT NULL DEFAULT 0, correct INTEGER NOT NULL DEFAULT 0, attempted INTEGER NOT NULL DEFAULT 0, total INTEGER NOT NULL DEFAULT 0, time_seconds INTEGER NOT NULL DEFAULT 0, completed_at TIMESTAMPTZ NOT NULL DEFAULT now())