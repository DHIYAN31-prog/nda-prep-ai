CREATE TABLE IF NOT EXISTS profiles (user_id TEXT PRIMARY KEY, name TEXT, language TEXT NOT NULL DEFAULT 'English', study_minutes INTEGER NOT NULL DEFAULT 60, reminder_interval INTEGER NOT NULL DEFAULT 60, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now())